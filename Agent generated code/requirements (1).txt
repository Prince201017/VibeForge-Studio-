# [V0.A7] Export Pipeline — backend dependencies
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
Pillow>=10.2
Jinja2>=3.1
httpx>=0.27
psutil>=5.9

# Optional — only needed for specific formats; the affected exporter raises
# a clear error at export time (not at import time) if these are missing.
pillow-avif-plugin>=1.4     # AVIF image sequence export
# OpenEXR>=1.3.9            # EXR sequence export — needs system libopenexr-dev first
# boto3>=1.34                # S3 storage upload target
# numpy>=1.26                # EXR export + PSNR/SSIM quality metrics
# scikit-image>=0.22         # PSNR/SSIM quality metrics
# lottie>=0.7                # optional Lottie validation pass only

# System dependency (not pip-installable): ffmpeg must be on PATH for all
# video formats (MP4/WebM/MOV/MKV/AVI).
